# Database configuration (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/powershift?schema=public"

# Server configuration
PORT=5000
NODE_ENV=development

# Authentication secrets (JWT)
JWT_SECRET="powershift_jwt_access_secret_2026_change_me_in_production"
REFRESH_SECRET="powershift_jwt_refresh_secret_2026_change_me_in_production"
